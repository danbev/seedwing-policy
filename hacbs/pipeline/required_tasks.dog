pattern pipeline = *data::from<"rule_data.yml">

pattern at-least-one-task = {
  kind: "Pipeline",
  spec: {
    tasks: list::any<{
        taskRef: {
          bundle: string,
          kind: string,
          name: string
        }
    }>,
  }
}
